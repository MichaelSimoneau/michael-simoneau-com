# The 5-State Zeroth Law Correction

This specification is aligned to the canonical correction text in `public/zeroth.txt`.

## Canonical Source

- `/public/zeroth.txt`

## Required Invariants

- Binary duality is rejected as incomplete.
- Tensor zeros are distinct: Negative Zero (`-0`), Positive Zero (`+0`), Unsigned Zero (`0`).
- Protocol0 must reject transitions that fail the 10% convergence delta toward Unsigned Zero (`0`).
